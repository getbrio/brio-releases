---
sidebar_position: 1
slug: /
---

# Welcome to BRIO

**BRIO is the AI dev tool for robotics engineers.**

It connects to your live ROS 2 environment, reads your active topic graph,
transform tree, diagnostics, and hardware constraints, and reasons about
your robot the way a senior teammate would — then writes, builds, and
verifies code that runs on the actual machine.

## What BRIO does

- **Sees your robot, not just your code.** BRIO snapshots your live
  workspace — TF tree, node list, `/diagnostics`, custom message types —
  and feeds that state to a Claude-powered supervisor agent.
- **Knows your hardware by name.** Jetson power envelope, carrier-board
  specs, sensor constraints, latency budgets. Generic AI doesn't know
  your 5V PAB requirement. BRIO does.
- **Ships code that builds first try.** Nodes with correct message types
  and QoS, launch files wired to your package, package config that
  matches your stack — generated, built, and verified against your real
  workspace.

## How it's put together

BRIO is two pieces:

| Piece | Where it runs | What it does |
| --- | --- | --- |
| **`brio` CLI** | Operator workstation (macOS / Linux) | 3-zone TUI. Sends queries to the cloud, executes agent tool calls (`bash` / `read_file` / `write_file`) locally over MCP/SSE. |
| **Cloud API** | `api.getbrio.org` | Hosts the supervisor agent (with explorer + coder sub-agents), handles auth, billing, and per-user API keys. |

A Jetson-side ROS 2 node can optionally stream live `RobotState`
(TF + diagnostics + node list) into the same session so the agent
reasons over what your robot is *actually* doing, not what your code
says it should.

```
┌────────────────────┐       RobotState + query        ┌─────────────────────┐
│ Jetson ROS 2 node  │ ──────────────────────────────▶ │  BRIO cloud API     │
│ TF / diagnostics   │ ◀──── streamed agent reply ──── │  Claude supervisor  │
└────────────────────┘                                 └─────────┬───────────┘
                                                                 │ MCP / SSE
                                                                 ▼
                                                       ┌─────────────────────┐
                                                       │ brio CLI (operator) │
                                                       │ bash / read / write │
                                                       └─────────────────────┘
```

## Who it's for

Robotics engineers who are done losing days to problems that shouldn't
take days — solo engineers shipping client projects, teams that want
the velocity web devs take for granted, anyone tired of `Build → Launch
→ Observe` for every line change.

## Next

- [Install the CLI](./installation.md) — one-line install for macOS and Linux.
- [Configure your account](./configuration.md) — sign in, plans, robot IDs, env overrides.
